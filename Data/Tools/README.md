# Data/Tools

Per-tool data for **Agenda**, **Projects**, and **Repository** capture (`New/`). This demo vault includes **sample missions, inbox items, projects, and New/ notes**.

The **big Repository tree** (six bases × six regions, plus boards) lives under **`Data/`** at vault root — see **`../README.md`** (parent `Data/` folder).
